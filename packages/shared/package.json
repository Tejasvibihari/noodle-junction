{
  "name": "@nj/shared",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Shared enums, error codes, money helpers and zod base schemas for server and client",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "clean": "rm -rf coverage"
  },
  "dependencies": {
    "zod": "^4.6.5"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vitest": "^5.0.1"
  }
}
